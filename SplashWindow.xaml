<Window x:Class="NavajaSuizaPDF.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cargando..." Height="300" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" Background="Transparent" AllowsTransparency="True">

    <Border CornerRadius="20" Background="#0F172A" BorderBrush="#3B82F6" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#2563EB" BlurRadius="40" ShadowDepth="0" Opacity="0.4"/>
        </Border.Effect>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="⚡" FontSize="70" HorizontalAlignment="Center" Foreground="#60A5FA" Margin="0,0,0,20"/>
            
            <TextBlock Text="FIBRICI LABS" 
                       FontFamily="Segoe UI" FontWeight="Black" FontSize="36" 
                       Foreground="White" HorizontalAlignment="Center" />
            
            <TextBlock Text="Inicializando protocolos de seguridad..." 
                       FontFamily="Consolas" FontSize="14" 
                       Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,10,0,0"/>

            <ProgressBar IsIndeterminate="True" Height="4" Width="200" Margin="0,30,0,0" Foreground="#3B82F6" Background="#1E293B" BorderThickness="0"/>
        </StackPanel>
    </Border>
</Window>